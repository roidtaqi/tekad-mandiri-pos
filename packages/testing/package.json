{
  "name": "@kastur/testing",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Shared test helpers, added only when concrete cross-workspace reuse exists.",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
