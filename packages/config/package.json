{
  "name": "@kastur/config",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Shared non-secret application configuration boundary.",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
